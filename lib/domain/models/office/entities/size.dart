class OfficeSize {
  final int width;
  final int height;

  const OfficeSize({
    required this.width,
    required this.height,
  });

  @override
  String toString() => 'Size(width: $width, height: $height)';
}
